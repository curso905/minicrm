# Local: usa /api (proxy a backend). Producción: URL del backend.
VITE_API_URL=/api

# Stripe: Price ID del producto (Dashboard → Product catalog → Basico → copiar price_...)
VITE_STRIPE_PRICE_BASICO=
